# Port the Fastify server listens on.
PORT=3001

# Host to bind to.
HOST=0.0.0.0

# apps/web's local dev origin — allowed by CORS so the browser can call
# this API's endpoints during local development.
WEB_DEV_ORIGIN=http://localhost:5173
