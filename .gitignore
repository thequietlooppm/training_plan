# OS
.DS_Store

# Node
node_modules/
dist/
build/
.next/
out/
*.log
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Env / secrets
.env
.env.*
!.env.example
# apps/web/.env.production holds a public deployed API URL baked into the
# static build at build time — not a secret, same convention as
# .env.example, so it's checked in rather than ignored.
!.env.production

# Editor
.vscode/
.idea/
*.swp

# Native (later)
apps/ios/build/
apps/ios/DerivedData/
apps/ios/Pods/
apps/android/.gradle/
apps/android/build/
apps/android/local.properties

# Claude Code local overrides
.claude/settings.local.json

# Reference material, not project content
plan_examples/
