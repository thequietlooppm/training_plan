# packages

Code shared across platforms — the API contract, domain types, and validation
rules that the web app and (later) the native iOS/Android apps all depend on.

Empty for now. The first shared package is extracted here the moment a second
client (or a standalone backend) needs the same types — don't let domain logic
settle inside `apps/web/` first.
